# Supabase project settings (safe to expose the anon key in the browser).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# GIF provider key. Keep provider calls behind a serverless function before launch.
VITE_GIF_PROVIDER=

# Reserved for server-side jobs and moderation workers.
SUPABASE_SERVICE_ROLE_KEY=
